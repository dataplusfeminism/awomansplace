{
  "name": "womans-place-city",
  "version": "0.0.1",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "package": "svelte-kit package",
    "preview": "vite preview",
    "prepare": "svelte-kit sync",
    "predeploy": "gh-pages -d build",
    "deploy": "npm run build && gh-pages -d build && touch build/.nojekyll && git add build/.nojekyll && git commit -m 'Adding .nojekyll to disable Jekyll' && git push origin gh-pages"
  },
  "devDependencies": {
    "@sveltejs/adapter-auto": "^3.0.0",
    "@sveltejs/adapter-netlify": "^4.3.1",
    "@sveltejs/adapter-static": "^3.0.8",
    "@sveltejs/kit": "^2.0.1",
    "@sveltejs/vite-plugin-svelte": "^3.0.0",
    "autoprefixer": "^10.4.19",
    "gh-pages": "^6.3.0",
    "postcss": "^8.4.39",
    "svelte": "^4.2.7",
    "svelte-check": "^3.6.0",
    "tailwindcss": "^3.4.4",
    "tslib": "^2.4.1",
    "typescript": "^5.0.0",
    "vite": "^5.0.10"
  },
  "type": "module",
  "dependencies": {
    "sveltekit-i18n": "^2.4.2"
  }
}
